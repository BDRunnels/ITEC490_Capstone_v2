services:
  api:
    build: ./api
    container_name: siem_api_2
    ports:
      - "${API_PORT:-5000}:5000"          # Optional: expose for direct debugging
    volumes:
      - ./data:/data
    networks:
      - siem_network

  dashboard:
    build: ./dashboard
    container_name: siem_dashboard_2
    ports:
      - "3000:80"            # Dashboard at http://localhost:3000
    depends_on:
      - api
    networks:
      - siem_network

networks:
  siem_network:
    driver: bridge
